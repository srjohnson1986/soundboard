# Copy this file to keystore.properties (gitignored) and fill in real values.
# See docs/RELEASING.md for how to generate the keystore itself.
storeFile=/absolute/path/to/soundboard-release.jks
storePassword=changeme
keyAlias=soundboard-release
keyPassword=changeme
